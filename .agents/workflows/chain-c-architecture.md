# Chain C: Architecture Pipeline (Secure)

description: Deterministic 3-step Trade-off, Architecture, and DX sanity check.

1. Verify Antigravity Strict Mode is active and network access is denied.
2. Call @Gergely-Orosz-EM-Advice to execute Trade-off analysis.
3. - Evaluate `TRADE_OFF_STATE` and persist state to `task.md` Artifact.
4. Call @Guillermo-Rauch-Architecture to design Technical architecture using $TRADE_OFF_STATE.
5. - Evaluate `TECH_ARCH_STATE` and persist state to `task.md` Artifact.
6. Call @Theo-Browne-T3-Review to perform DX sanity check on $TECH_ARCH_STATE.
