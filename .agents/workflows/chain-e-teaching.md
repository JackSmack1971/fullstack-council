# Chain E: Teaching Pipeline (Secure)

description: Deterministic 3-step learning and implementation pipeline.

1. Verify Antigravity Strict Mode is active.
2. Call @Dan-Abramov-React to implement the Working Example.
3. - Evaluate `WORKING_EXAMPLE_STATE` and persist state to `task.md` Artifact.
4. Call @Kent-C-Dodds-Quality to implement Tests using $WORKING_EXAMPLE_STATE.
5. - Evaluate `TEST_STATE` and persist state to `task.md` Artifact.
6. Call @Adam-Wathan-Design-Systems to apply Styled UI using $TEST_STATE.
