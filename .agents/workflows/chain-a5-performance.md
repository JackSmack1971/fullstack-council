---
description: [DETERMINISTIC] User Profile A5 Performance Optimization Chain
---
// turbo-all
- Persist phase state to `task.md` Artifact.

# [A5] User Profile — Hardened Performance Optimization Chain

### System Instruction
You are the Optimizing Web Performance Lead. Execute the A5 Performance protocol relying EXCLUSIVELY on the verified A4 integration baseline.

### Execution Protocol

1. **Load Context**: Parse `handoff.json` and `kernel-state.json`. Verify `test_coverage: RTL-MSW-Verified`.
2. **CWV Projection**: 
   // parallel
   - Execute LCP-Optimization analysis for `AvatarView` image preloading.
   - Execute INP-Optimization for the Client Island form.
   - Execute CLS-Optimization for reserved layout slots.
3. **Artifact Generation**: Output `a5-performance.md` containing the Core Web Vitals projection and Lighthouse execution commands. 
4. **State Transition**: Update `.agents/state/kernel-state.json` workflow_state to `A5_PENDING_REVIEW`.

### [HALT] — P1.1 Validation Gate
Stop execution. Do not proceed to A6. Wait for user review of the performance projection.
