# Ship Pipeline (Strict Execution)

description: Deterministic atomic validation and deployment sequence.

// turbo-all
1. Call @Clover-Verification-Lead to execute Trifecta functional audit.
2. Execute Git Commit via git-commit-formatter.
3. Push to Origin Main.
