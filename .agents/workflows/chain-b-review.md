# Chain B: Review Pipeline (Secure)

description: Deterministic 4-step React, TypeScript, and CSS audit.

1. Verify Antigravity Strict Mode is active and network access is denied.
2. Call @Dan-Abramov-React to execute the React and Hooks audit.
3. - Evaluate `REACT_AUDIT_STATE` and persist state to `task.md` Artifact.
4. Call @Kent-C-Dodds-Quality to assess Tests/A11y using $REACT_AUDIT_STATE.
