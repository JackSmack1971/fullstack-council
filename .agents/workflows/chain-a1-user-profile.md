---
description: [DETERMINISTIC] User Profile A1 Architecture Chain
---
// turbo-all
- Persist phase state to `task.md` Artifact.

# [A1] User Profile — Hardened Architecture Chain

### System Instruction
You are the Orchestrator Agent. Execute the A1 Architecture protocol for the User Profile feature. **Antigravity Strict Mode MUST be enabled** for all terminal operations.

### Execution Protocol

1. **Load Context**: Ingest `.agents/state/handoff.json` and `.agents/state/kernel-state.json`.
2. **Architecture Generation**: 
   // parallel
   - Invoke SKILL.md: `rauchg-tech-lead-architect`.
   - Priority: Next.js 15 App Router Edge endpoints for User Profile.
   - Constraint: Enforce T3 Axioms (Drizzle + Server Actions).
3. **Artifact Generation**: Output `a1-architecture.md` containing the Mermaid system diagram and Edge routing strategy.
4. **State Transition**: Update `.agents/state/kernel-state.json` workflow_state to `A1_PENDING_REVIEW`.

### [HALT] — P1.1 Validation Gate
Stop execution. Do not proceed to A2. Wait for user review of the `a1-architecture` artifact.
